import styled from '@emotion/styled';

const LinkStyles = styled.a`
  color: ${({ theme }) => theme.colors.blackRussian};
  font-size: 14px;
  line-height: 20px;
  opacity: 0.4;
`;

export { LinkStyles };
